#include <iostream>
using namespace std;
class BinarySearchTree {
public:
	/*Binary_Tree<T>* searchBST(Binary_Tree<T>* root, int val) {
		if (root == NULL) {
			return NULL;
		}
		if (root->data == val)
			return root;
		else if (root->data > val) {
			return searchBST(root->leftNode, val);
		}
		else
			return searchBST(root->rightNode, val);
	}*/
	Binary_Tree<int>* searchBST(int val) {
		return NULL;
	}
};