#include <iostream>
using namespace std;
class IntToRoman {
public:
	IntToRoman(int num) {
        string output;
        if ((num / 1000) >= 1) {
            int divisionVal = num / 1000;
            int remainder = (num / 1000) % 1000;
        }
	}
};