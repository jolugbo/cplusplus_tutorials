#include <vector>
#include <algorithm>
using namespace std;
class threeSum {
public:
	threeSum(vector<int>& nums) {
		vector< vector<int>> output;
		sort(nums.begin(), nums.end());
		int size = nums.size();
		int start = 1;
		int end = size -  1;
		for (int i = 0; i < size; i++)
		{
			while (true)
			{

			}
		}
	}
};